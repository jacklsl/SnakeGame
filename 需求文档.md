# 贪吃蛇2D游戏 - 需求文档

## 1. 项目概述

### 1.1 项目简介
基于 Unity 6000.4.5f1 开发的经典贪吃蛇2D游戏，采用 URP（通用渲染管线）渲染，支持键盘和触控操作。

### 1.2 技术栈
- **引擎版本**：Unity 6000.4.5f1
- **渲染管线**：URP（Universal Render Pipeline）
- **开发语言**：C#
- **目标平台**：Windows / macOS / 移动端（Android/iOS）

---

## 2. 核心玩法

### 2.1 基本规则
- 玩家控制一条蛇在网格地图上移动
- 蛇会持续前进，玩家控制其转向（上、下、左、右）
- 地图上随机生成食物，蛇吃到食物后身体长度+1，得分增加
- 蛇撞到墙壁或自己的身体则游戏结束

### 2.2 移动机制
- 蛇的移动基于网格（Grid），每次移动一个单元格
- 蛇不能反向移动（例如正在向右时不能立即向左）
- 移动速度可随游戏进程逐渐加快，增加难度

### 2.3 食物系统
- 食物在地图空白位置随机生成
- 每次只存在一个食物
- 蛇头碰到食物即判定为吃到
- 吃到食物后立即在随机空白位置生成新食物

### 2.4 碰撞检测
- **墙壁碰撞**：蛇头超出地图边界 → 游戏结束
- **自身碰撞**：蛇头碰到自身身体任意部分 → 游戏结束

---

## 3. 游戏功能

### 3.1 游戏状态管理
| 状态 | 说明 |
|------|------|
| 待开始（Ready） | 游戏启动后的初始状态，显示开始界面 |
| 进行中（Playing） | 游戏正在运行，蛇可移动 |
| 暂停（Paused） | 游戏暂停，显示暂停菜单 |
| 结束（GameOver） | 蛇死亡，显示结算界面 |

### 3.2 计分系统
- 每吃到一个食物得 1 分
- 显示当前得分
- 记录并显示历史最高分（本地持久化存储）

### 3.3 难度递增
- 每吃 5 个食物，蛇的移动速度提升一个档次
- 速度提升应有上限，避免过快导致无法操作

### 3.4 音效与视觉反馈
- 吃到食物时播放音效
- 游戏结束时播放音效
- 蛇移动时身体跟随平滑（可选）

---

## 4. 界面设计

### 4.1 主界面（开始界面）
- 游戏标题 "贪吃蛇"
- "开始游戏" 按钮
- "最高分" 显示

### 4.2 游戏界面
- 网格地图区域（居中）
- 左上角显示当前得分
- 右上角显示最高分
- 暂停按钮（右上角或左上角）

### 4.3 暂停界面
- "继续游戏" 按钮
- "重新开始" 按钮
- "返回主菜单" 按钮

### 4.4 游戏结束界面
- 显示 "游戏结束"
- 显示本局得分
- 显示最高分（如有新纪录则高亮提示）
- "重新开始" 按钮
- "返回主菜单" 按钮

---

## 5. 技术设计

### 5.1 地图系统
- 使用 Grid 组件实现网格化地图
- 地图尺寸：20×20 单元格（可配置）
- 每个单元格大小：1×1 单位
- 地图边界用墙壁装饰

### 5.2 蛇的表示
- 蛇由多个 Segment（节段）组成
- 蛇头：使用单独的游戏对象，带方向指示
- 蛇身：使用列表（List）管理每个节段的位置
- 移动逻辑：蛇头向当前方向移动一格，蛇身依次跟随前一个节段的位置

### 5.3 食物生成
- 使用随机算法在地图空白位置生成食物
- 食物不能生成在蛇身体占据的格子上

### 5.4 输入处理
- 键盘：WASD 或 方向键控制方向
- 触控：滑动屏幕控制方向（移动端）
- 使用 Unity 新输入系统（Input System）

### 5.5 数据持久化
- 使用 PlayerPrefs 存储最高分
- 可选：存储游戏设置（音效开关等）

---

## 6. 项目结构

```
Assets/
├── Scenes/                    # 场景文件
│   ├── MainMenu.unity         # 主菜单场景
│   └── Game.unity             # 游戏场景
├── Scripts/                   # 脚本文件
│   ├── Managers/              # 管理器
│   │   ├── GameManager.cs     # 游戏状态管理
│   │   ├── InputManager.cs    # 输入处理
│   │   └── ScoreManager.cs    # 计分管理
│   ├── Snake/                 # 蛇相关
│   │   ├── SnakeController.cs # 蛇的控制逻辑
│   │   └── SnakeSegment.cs    # 蛇节段组件
│   ├── Food/                  # 食物相关
│   │   └── FoodSpawner.cs     # 食物生成器
│   ├── Grid/                  # 地图相关
│   │   └── GridManager.cs     # 网格管理
│   └── UI/                    # UI相关
│       ├── UIManager.cs       # UI管理
│       └── ScoreDisplay.cs    # 分数显示
├── Prefabs/                   # 预制体
│   ├── SnakeHead.prefab       # 蛇头预制体
│   ├── SnakeBody.prefab       # 蛇身预制体
│   └── Food.prefab            # 食物预制体
├── Sprites/                   # 精灵资源
│   ├── SnakeHead.png          # 蛇头图片
│   ├── SnakeBody.png          # 蛇身图片
│   ├── Food.png               # 食物图片
│   └── Wall.png               # 墙壁图片
├── Audio/                     # 音效资源
│   ├── Eat.wav                # 吃东西音效
│   └── GameOver.wav           # 游戏结束音效
└── Fonts/                     # 字体资源
    └── GameFont.ttf           # 游戏字体
```

---

## 7. 开发路线图

### 第一阶段：核心功能（MVP）
- [ ] 搭建网格地图系统
- [ ] 实现蛇的移动逻辑（键盘控制）
- [ ] 实现食物生成与碰撞检测
- [ ] 实现墙壁碰撞与自身碰撞检测
- [ ] 实现计分系统
- [ ] 实现基础UI（得分显示、游戏结束界面）

### 第二阶段：完善体验
- [ ] 添加主菜单界面
- [ ] 添加暂停功能
- [ ] 实现最高分持久化存储
- [ ] 添加难度递增机制
- [ ] 添加音效

### 第三阶段：优化与扩展
- [ ] 添加触控滑动控制（移动端适配）
- [ ] 添加动画效果（蛇身平滑移动）
- [ ] 添加视觉特效（粒子效果等）
- [ ] 添加设置界面（音效开关、难度选择）
- [ ] 添加多种地图模式（可选）

---

## 8. 非功能性需求

### 8.1 性能
- 游戏运行帧率稳定在 60 FPS
- 内存占用控制在合理范围内

### 8.2 兼容性
- 支持 16:9 和 9:16 屏幕比例自适应
- 支持键盘和触控两种输入方式

### 8.3 可维护性
- 代码遵循单一职责原则，模块化设计
- 使用事件系统实现模块间解耦
- 关键参数可通过 Inspector 配置

---

## 9. 附录

### 9.1 参考资源
- 项目已有资源：`Assets/Sprite/` 目录下的精灵图可作为临时占位资源
- 项目已有忍者女孩素材（`Assets/NinjaGirl/`）暂不用于此项目

### 9.2 术语表
| 术语 | 说明 |
|------|------|
| Grid | 网格系统，用于蛇的移动定位 |
| Segment | 蛇的节段，蛇头和蛇身的总称 |
| URP | Unity Universal Render Pipeline，通用渲染管线 |
| MVP | Minimum Viable Product，最小可行产品 |